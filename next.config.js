/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuración de imágenes para permitir dominios externos en el futuro
  images: {
    domains: [],
  },
}

module.exports = nextConfig
